{
  "name": "@pumps/desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/bundle-api.mjs && node scripts/copy-assets.mjs",
    "package": "electron-builder --win",
    "dist": "electron-builder --win"
  },
  "dependencies": {
    "electron": "^38.2.0"
  },
  "devDependencies": {
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.2",
    "esbuild": "^0.28.2"
  }
}