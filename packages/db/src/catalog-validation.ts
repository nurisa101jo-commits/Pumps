import type {CatalogStore} from "./catalog-service.js";
import {validateCurvePoints} from "@pumps/domain/curve";
export function validateConfiguration(store:CatalogStore,input:any){if(!store.models.has(input.modelId))throw new Error("modelId does not exist");if(!store.motors.has(input.motorId))throw new Error("motorId does not exist");if(input.curveId&&!store.curves.has(input.curveId))throw new Error("curveId does not exist");return true}
export function validateCurve(store:CatalogStore,input:any){if(!store.configurations.has(input.configurationId))throw new Error("configurationId does not exist");if(!["head","efficiency","power","npsh"].includes(input.kind))throw new Error("Invalid curve kind");if(!Number.isFinite(input.speedRpm)||!Number.isFinite(input.frequencyHz))throw new Error("speedRpm and frequencyHz are required");validateCurvePoints(input.points);return true}
export function validateDimension(store:CatalogStore,input:any){if(!store.configurations.has(input.configurationId))throw new Error("configurationId does not exist");return true}
