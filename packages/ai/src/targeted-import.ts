import type {ExtractedPumpCatalog,ExtractedConfiguration,ExtractedCurve,ExtractedDimensions,ExtractedMotor} from "./catalog-schema.js";
export type ImportScope="catalog"|"curves"|"dimensions"|"motors"|"materials"|"seals"|"configurations";
export type TargetedExtraction={scope:ImportScope;catalog?:Partial<ExtractedPumpCatalog>;configurations?:ExtractedConfiguration[];curves?:ExtractedCurve[];dimensions?:ExtractedDimensions[];motors?:ExtractedMotor[]};
export function validateTargetedScope(scope:ImportScope,data:any){if(scope==="catalog")return data;if(!data||typeof data!=="object")throw new Error("Targeted extraction must return an object");const allowed:Record<ImportScope,string[]>={catalog:[],curves:["curves"],dimensions:["dimensions"],motors:["motors"],materials:["materials"],seals:["seals"],configurations:["configurations"]};const keys=Object.keys(data).filter(k=>!["series","models","sources"].includes(k));for(const key of keys)if(!allowed[scope].includes(key))throw new Error("AI returned data outside requested import scope: "+key);return data}
