import type {ConfigurationContext,EngineeringRule,RuleEvaluation} from "@pumps/domain/rules";
export function evaluateRules(context:ConfigurationContext,rules:EngineeringRule[]):RuleEvaluation[]{return rules.filter(r=>r.enabled).map(rule=>{let passed=true;let message="Rule passed";const p=rule.parameters;switch(rule.kind){case"temperature_limit":if(context.temperatureC!==undefined&&typeof p.maxTemperatureC==="number"&&context.temperatureC>p.maxTemperatureC){passed=false;message=`Temperature ${context.temperatureC}°C exceeds limit ${p.maxTemperatureC}°C`}break;case"flow_limit":if(typeof p.minFlow==="number"&&context.flow<p.minFlow){passed=false;message=`Flow is below minimum ${p.minFlow}`}if(typeof p.maxFlow==="number"&&context.flow>p.maxFlow){passed=false;message=`Flow exceeds maximum ${p.maxFlow}`}break;case"npsh_limit":if(context.npshr!==undefined&&typeof p.maxNpshr==="number"&&context.npshr>p.maxNpshr){passed=false;message=`NPSH required ${context.npshr} exceeds limit ${p.maxNpshr}`}break;case"motor_compatibility":if(typeof p.allowedPowerKw==="object"&&Array.isArray(p.allowedPowerKw)&&!p.allowedPowerKw.includes(context.motor.powerKw)){passed=false;message=`Motor power ${context.motor.powerKw} kW is not permitted`}break;}return {ruleId:rule.id,passed,severity:rule.severity,message}})}
