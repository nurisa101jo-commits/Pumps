import type {SelectionRequest,SelectionResult} from "@pumps/domain";
import {interpolate} from "@pumps/domain/curve";
export type CatalogConfiguration={id:string;modelId:string;motorId:string;head:Array<{q:number;value:number}>;efficiency?:Array<{q:number;value:number}>;npsh?:Array<{q:number;value:number}>};
export function selectFromCatalog(request:SelectionRequest,catalog:CatalogConfiguration[]):SelectionResult{const warnings:string[]=[];const candidates=catalog.flatMap(c=>{if(request.constraints?.allowedConfigurationIds&&!request.constraints.allowedConfigurationIds.includes(c.id))return [];const dutyResults=request.dutyPoints.map(d=>{const h=interpolate(c.head,d.q);const e=c.efficiency?interpolate(c.efficiency,d.q):undefined;const n=c.npsh?interpolate(c.npsh,d.q):undefined;return {q:d.q,requiredHead:d.head,availableHead:h??NaN,headError:h===undefined?Infinity:Math.abs(h-d.head),efficiency:e,npshr:n}});if(dutyResults.some(x=>!Number.isFinite(x.availableHead)))return [];if(request.constraints?.maxNpshr!==undefined&&dutyResults.some(x=>x.npshr!==undefined&&x.npshr>request.constraints!.maxNpshr!))return [];if(request.constraints?.minEfficiency!==undefined&&dutyResults.some(x=>x.efficiency!==undefined&&x.efficiency<request.constraints!.minEfficiency!))return [];const score=dutyResults.reduce((s,x)=>s+x.headError,0)/dutyResults.length;return [{configurationId:c.id,modelId:c.modelId,motorId:c.motorId,score,dutyResults}]});candidates.sort((a,b)=>a.score-b.score);return {candidates,warnings,engineVersion:"0.2.0"}}
export const selectionEngine={select:(request:SelectionRequest)=>selectFromCatalog(request,[])};
